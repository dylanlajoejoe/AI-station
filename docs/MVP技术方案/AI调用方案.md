# AI 调用方案

## 1. 技术栈

- OpenAI 兼容接口
- Node.js 服务端调用

## 2. 调用方式

前端发送用户消息到本地服务，本地服务调用 AI 接口，再把结果返回前端。

## 3. 发送内容

只发送用户输入框里的文字和必要的会话消息。

如果用户通过 `添加到对话` 插入了文件名，也只作为普通文字发送。

## 4. 不发送内容

- 不发送文件内容
- 不发送整个目录结构
- 不发送未选择目录的信息

## 5. 异常处理

- AI 接口失败时提示用户重试
- AI 接口超时时提示网络或服务异常
- API Key 缺失时提示用户配置
